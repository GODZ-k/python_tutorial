# t=(10,20,30,40)   # tuple is a immutable datatype means that we can not detete insert and modify the tuple after define .
# if tuple contains only one value it can not be tuple it will be int or string .
# a=max(t)
# a=min(t)
# a=t.count(10)
# a=t.index(40)
# a=sum(t)  # sum of all the values of the tuples
# a=sum(t,10)  # after sum add additional value 10 in the result
# print(a)